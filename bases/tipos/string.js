"use strict";
(() => {
    var _a;
    const batman = 'Batman';
    const linternaverde = "linterna verde";
    const volcanoNegro = `Volcano negro`;
    console.log(`I'am ${batman.toUpperCase()}`);
    console.log(`I'am ${((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'Superman'}`);
})();
